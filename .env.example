# Example .env for Northshore Books
# Copy this to .env and fill with real values (do NOT commit .env)

SECRET_KEY=replace-with-a-secret
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

DB_ENGINE=django.db.backends.mysql
DB_NAME=northshorebooks
DB_USER=booksuser
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=3306
